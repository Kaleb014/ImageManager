<UserControl x:Class="ImageManager.Views.UserControls.DragPopupUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageManager.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Popup
      x:Name="DragRowPopup"
      IsHitTestVisible="False"
      Placement="RelativePoint"
      PlacementTarget="{Binding ElementName=ImageManagerWindow}"
      AllowsTransparency="True">
        <Border
            BorderBrush="Gray"
            BorderThickness="2"
            Background="White"
            Opacity="0.75">
            <StackPanel
                Orientation="Horizontal"
                Margin="4,3,8,3">
                <Image
                    Source="/Resources/Icons/folder.png"
                    Width="16"
                    Height="16" />
                <TextBlock
                    FontWeight="Bold"
                    VerticalAlignment="Center"
                    Text="{Binding SelectedIMItem.Name}"
                    Margin="8,0,0,0" />
            </StackPanel>
        </Border>
    </Popup>
</UserControl>
